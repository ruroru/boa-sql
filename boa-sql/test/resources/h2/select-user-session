SELECT
    username,
    session_id AS "session-id",
    FORMATDATETIME(PARSEDATETIME(creation_date, 'yyyy-MM-dd HH:mm:ss'), 'yyyy-MM-dd HH:mm:ss') AS "creation-date"
FROM
    usersessions
WHERE session_id = :session;